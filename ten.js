"use strict";
// Program written by [Omar]
// Date: [2-Nov-2023]
const famousperson = "Albert Einstein"; // Store the name of the famous person
const famQuote = "A person who never made a mistake never tried anything new."; // Store the famous quote
// Compose the message that includes the famous person's name and the quoted text
const meSSage = `${famousperson} once said, "${famQuote}"`;
// Print the message
console.log(meSSage);
// Program written by [Omar]
// Date: [2-Nov-2023]
const nameWhitespace = "\t \n   John Doe   \n \t"; // Name with leading and trailing whitespace
// Print the name with leading and trailing whitespace
console.log("Name with Whitespace: \"" + nameWhitespace + "\"");
// Remove leading and trailing whitespace using the trim() method
const stripName = nameWhitespace.trim();
// Print the stripped name
console.log("Stripped Name: \"" + stripName + "\"");
