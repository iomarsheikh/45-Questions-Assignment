const famous_person: string = "Albert Einstein";
const hisQuote: string = "A person who never made a mistake never tried anything new.";

const msg: string = `${famous_person} once said, "${hisQuote}"`;

console.log(msg);